{
  "name": "camshadow-mvp",
  "version": "0.1.0",
  "description": "CamShadow camera-only floating overlay",
  "scripts": {
    "start": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  }
}
