{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "default capability",
  "windows": ["control", "camera"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
